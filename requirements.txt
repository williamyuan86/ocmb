# OCMB Requirements

# Core dependencies
numpy>=1.20.0
pandas>=1.3.0
scipy>=1.7.0
scikit-learn>=1.0.0
networkx>=2.6
torch>=1.10.0

# Optional: GPU acceleration for CMI tests
numba>=0.54.0

# Optional: LightGBM for CaPS_LGBM variant
lightgbm>=3.3.0

# Optional: CUDA support (install manually if needed)
# torch-cuda (follow PyTorch installation guide for your CUDA version)

# Development dependencies (optional)
tqdm>=4.62.0
